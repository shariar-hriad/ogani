import React from 'react';
import Navbar from './Navbar';
import TopBar from './TopBar';

const Header = () => {
   return (
      <header>
         <TopBar />
         <Navbar />
      </header>
   );
};

export default Header;
